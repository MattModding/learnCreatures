{
  "values": [
    "minecraft:grove",                                <= single biome
    "#minecraft:is_forest",                           <= other biome tag (vanilla tag)
    "#c:forest",                                      <= fabric biome tag
    {
      "id": "twilightforest:firefly_forest",          <= optional biome (should be used with external mods)
      "required": false
    }
  ]
}
